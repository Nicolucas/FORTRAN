c Subroutine that saves two arrays into a *.txt
      subroutine SaveMeMaybe()
        Double Precision CosResult(100)
        integer tmp

        tmp = a
        a = b
        b = tmp

        return
      end